---
import "../styles/global.css";

const { title, subtitle, symbol } = Astro.props;
const currentPath = Astro.url.pathname;

const navLinks = [
    { href: "/", label: "HOME" },
    { href: "/new-testament/", label: "NEW TEST." },
    { href: "/old-testament/", label: "OLD TEST." },
    { href: "/lilith/", label: "LILITH" },
    { href: "/demonology/", label: "DEMONS" },
    { href: "/symbolism/", label: "SYMBOLS" },
    { href: "/lexicon/", label: "LEXICON" },
];
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{title} | Silent Hill Biblical Themes</title>
    </head>
    <body>
        <nav>
            <ul>
                {
                    navLinks.map((link) => (
                        <li>
                            <a
                                href={link.href}
                                class={
                                    currentPath === link.href ? "active" : ""
                                }
                            >
                                {link.label}
                            </a>
                        </li>
                    ))
                }
            </ul>
        </nav>

        {
            title !== "Silent Hill" && (
                <header>
                    <div class="symbol">{symbol}</div>
                    <h1>{title}</h1>
                    <div class="subtitle">{subtitle}</div>
                </header>
            )
        }

        <div class="container">
            <main>
                <slot />
            </main>
        </div>

        <footer>
            <div class="motto">☩ IN STERQUILINIIS INVENITUR ☩</div>
            <div class="translation">"In the dirt, it will be found"</div>
        </footer>
    </body>
</html>
